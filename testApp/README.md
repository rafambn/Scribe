# testApp

Standalone Amper Compose Multiplatform sample targeting Android, iOS, and JVM desktop.

## Run

From the `testApp` directory:

```bash
./amper run -m jvm-app
./amper run -m android-app
./amper run -m ios-app
```

Open the folder in IntelliJ IDEA with the Amper plugin for the best IDE support.
