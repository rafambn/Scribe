{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Scribe","text":"A Kotlin Multiplatform logging library for structured events and long-lived contextual flows."},{"location":"#why-scribe","title":"Why Scribe","text":"<p>Modern systems do not fail in one place. A single user action can cross services, queues, retries, feature flags, and third-party calls. Traditional logging turns that into dozens of partial lines, each easy to write and hard to query.</p> <p>Scribe is built around the argument from loggingsucks.com: useful logs are not just structured, they are context-rich. Instead of scattering breadcrumbs everywhere, Scribe gives you primitives for both shapes you actually need:</p> <ul> <li><code>Note</code> for one-off events that stand on their own (Old way, if you still insist)</li> <li><code>Scroll</code> for building a wide event over the lifetime of an operation, then sealing it as a <code>SealedScroll</code></li> </ul> <p>That pushes logging toward \"what happened to this request or workflow?\" instead of \"what line of code ran next?\". The result is fewer events, better context, and logs that are easier to filter, correlate, and analyze.</p>"},{"location":"#read-next","title":"Read Next","text":"<ul> <li>Getting Started for setup and first usage</li> <li>API Concepts for terminology and data model</li> <li>Lifecycle and Delivery for buffering, margins, and shutdown</li> </ul>"},{"location":"api-concepts/","title":"API Concepts","text":""},{"location":"api-concepts/#core-types","title":"Core Types","text":"<p>Scribe models logging with two event shapes:</p> <ul> <li><code>Note</code>: a single standalone event</li> <li><code>SealedScroll</code>: the finalized result of a multi-step <code>Scroll</code></li> </ul> <p>Both implement the sealed <code>Entry</code> interface, which is what <code>EntrySaver</code> receives.</p>"},{"location":"api-concepts/#terminology","title":"Terminology","text":"<ul> <li><code>note(...)</code>: suspending call for a single log entry</li> <li><code>flingNote(...)</code>: non-suspending best-effort note dispatch</li> <li><code>unrollScroll(...)</code>: starts a contextual logging session</li> <li><code>seal(...)</code>: finalizes a scroll and emits a <code>SealedScroll</code></li> <li><code>looseSeal(...)</code>: non-suspending best-effort seal</li> <li><code>Margin</code>: hook for writing fields at open/close boundaries</li> <li><code>ScribeDeliveryConfig</code>: queue size, overflow strategy, and saver error handling</li> </ul>"},{"location":"api-concepts/#scribe","title":"<code>Scribe</code>","text":"<p><code>Scribe</code> is the entry point. It owns:</p> <ul> <li>one or more savers (<code>shelf</code> or <code>shelves</code>)</li> <li>an optional shared <code>imprint</code></li> <li>delivery behavior through <code>ScribeDeliveryConfig</code></li> <li>optional lifecycle hooks through <code>Margin</code></li> </ul> <p>You can inspect active scrolls with <code>seekScrolls()</code> and create a new one with <code>unrollScroll()</code>.</p>"},{"location":"api-concepts/#scroll","title":"<code>Scroll</code>","text":"<p><code>Scroll</code> collects structured fields until it is sealed.</p> <pre><code>val scroll = scribe.unrollScroll(\"checkout-42\")\nscroll.writeString(\"gateway\", \"stripe\")\nscroll.writeNumber(\"attempt\", 1)\nscroll.writeBoolean(\"retry\", false)\n</code></pre> <p>The API is intentionally explicit:</p> <ul> <li><code>writeString(...)</code></li> <li><code>writeNumber(...)</code></li> <li><code>writeBoolean(...)</code></li> <li><code>writeSerializable(...)</code></li> <li><code>read(...)</code></li> <li><code>erase(...)</code></li> </ul> <p>After a scroll is sealed, writes and erases stop mutating it.</p>"},{"location":"api-concepts/#margin","title":"<code>Margin</code>","text":"<p><code>Margin</code> enriches a scroll at beginning and end.</p> <pre><code>val timingMargin = object : Margin {\n    override fun header(scroll: Scroll) {\n        scroll.writeNumber(\"startedAtEpochMs\", 1000L)\n    }\n\n    override fun footer(scroll: Scroll) {\n        scroll.writeNumber(\"sealedAtEpochMs\", 2000L)\n    }\n}\n</code></pre>"},{"location":"api-concepts/#delivery-configuration","title":"Delivery Configuration","text":"<pre><code>val scribe = Scribe(\n    shelf = entrySaver,\n    deliveryConfig = ScribeDeliveryConfig(\n        bufferSize = 256,\n        overflowStrategy = BufferOverflow.DROP_OLDEST,\n        onSaverError = { saver, entry, error -&gt;\n            println(\"Saver $saver failed for $entry: $error\")\n        }\n    )\n)\n</code></pre>"},{"location":"api-concepts/#event-shapes","title":"Event Shapes","text":"<pre><code>Note(\n    tag = \"payments\",\n    message = \"starting checkout\",\n    level = Urgency.INFO,\n    timestamp = 1710000000000L,\n)\n</code></pre> <pre><code>SealedScroll(\n    scrollId = \"checkout-42\",\n    success = true,\n    errorMessage = null,\n    context = mapOf(),\n    data = mapOf(),\n)\n</code></pre>"},{"location":"api-concepts/#failure-handling","title":"Failure Handling","text":"<pre><code>val scribe = Scribe(\n    shelf = entrySaver,\n    onIgnition = { throwable -&gt;\n        println(\"Uncaught exception: ${throwable.message}\")\n    }\n)\n</code></pre> <p><code>onIgnition</code> handles uncaught exceptions at the platform level. Saver failures are handled separately through <code>ScribeDeliveryConfig.onSaverError</code>.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#add-scribe-to-commonmain","title":"Add Scribe to <code>commonMain</code>","text":"<p>Use the library from shared code in your Kotlin Multiplatform module:</p> <pre><code>kotlin {\n    sourceSets {\n        commonMain.dependencies {\n            implementation(\"com.rafambn:scribe:0.1.0\")\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/#create-a-minimal-scribe","title":"Create a Minimal <code>Scribe</code>","text":"<p>The smallest setup is a single saver. A <code>NoteSaver</code> only receives <code>Note</code> events:</p> <pre><code>val scribe = Scribe(\n    shelf = NoteSaver { note -&gt;\n        println(\"[${note.level}] ${note.tag}: ${note.message}\")\n    }\n)\n</code></pre>"},{"location":"getting-started/#emit-a-single-event","title":"Emit a Single Event","text":"<p>Use <code>note(...)</code> when you can suspend and want delivery through the internal queue:</p> <pre><code>scribe.note(\n    tag = \"payments\",\n    message = \"starting checkout\",\n    level = Urgency.INFO,\n)\n</code></pre> <p>If you need best-effort fire-and-forget behavior, use <code>flingNote(...)</code>:</p> <pre><code>scribe.flingNote(\n    tag = \"payments\",\n    message = \"checkout queued\",\n    level = Urgency.DEBUG,\n)\n</code></pre> <p>With the saver above, the log output looks like this:</p> <pre><code>[INFO] payments: starting checkout\n</code></pre>"},{"location":"getting-started/#track-a-flow-with-scroll","title":"Track a Flow with <code>Scroll</code>","text":"<p><code>Scroll</code> is for multi-step operations where context accumulates before a final result is emitted.</p> <pre><code>val scribe = Scribe(\n    shelf = ScrollSaver { scroll -&gt;\n        println(scroll)\n    },\n    imprint = mapOf(\n        \"service\" to JsonPrimitive(\"billing\"),\n        \"environment\" to JsonPrimitive(\"production\"),\n    )\n)\n\nval scroll = scribe.unrollScroll(id = \"checkout-42\")\nscroll.writeString(\"gateway\", \"stripe\")\nscroll.writeNumber(\"attempt\", 1)\nscroll.writeBoolean(\"retry\", false)\nscroll.seal(success = true)\n</code></pre> <p>The emitted <code>SealedScroll</code> would look like this in JSON:</p> <pre><code>{\n  \"scrollId\": \"checkout-42\",\n  \"success\": true,\n  \"errorMessage\": null,\n  \"context\": {\n    \"service\": \"billing\",\n    \"environment\": \"production\"\n  },\n  \"data\": {\n    \"gateway\": \"stripe\",\n    \"attempt\": 1,\n    \"retry\": false\n  }\n}\n</code></pre> <p>Then in the saver you can manage this output whatever you want to save to you database or send to the cloud.</p>"},{"location":"getting-started/#choose-the-right-saver","title":"Choose the Right Saver","text":"<pre><code>val noteSaver = NoteSaver { note -&gt; println(note) }\nval scrollSaver = ScrollSaver { scroll -&gt; println(scroll) }\nval entrySaver = EntrySaver { entry -&gt; println(entry) }\n</code></pre> <ul> <li><code>NoteSaver</code> handles only <code>Note</code></li> <li><code>ScrollSaver</code> handles only <code>SealedScroll</code></li> <li><code>EntrySaver</code> handles both</li> </ul>"},{"location":"getting-started/#what-to-read-next","title":"What to Read Next","text":"<ul> <li>API Concepts for the core types and terminology</li> <li>Lifecycle and Delivery for buffering, margins, shutdown, and error handling</li> </ul>"},{"location":"lifecycle-and-delivery/","title":"Lifecycle and Delivery","text":""},{"location":"lifecycle-and-delivery/#delivery-pipeline","title":"Delivery Pipeline","text":"<p><code>Scribe</code> sends entries through an internal <code>Channel</code> before invoking savers. This gives you one place to tune buffering and overflow behavior.</p> <pre><code>val scribe = Scribe(\n    shelf = EntrySaver { entry -&gt;\n        println(entry)\n    },\n    deliveryConfig = ScribeDeliveryConfig(\n        bufferSize = 256,\n        overflowStrategy = BufferOverflow.DROP_OLDEST,\n        onSaverError = { saver, entry, error -&gt;\n            println(\"Saver $saver failed for $entry: ${error.message}\")\n        }\n    )\n)\n</code></pre>"},{"location":"lifecycle-and-delivery/#suspending-vs-best-effort-apis","title":"Suspending vs Best-Effort APIs","text":"<p>Choose based on backpressure and call-site constraints:</p> <ul> <li><code>note(...)</code> suspends while sending to the queue</li> <li><code>flingNote(...)</code> is best-effort and does not suspend</li> <li><code>seal(...)</code> suspends before dispatching the final <code>SealedScroll</code></li> <li><code>looseSeal(...)</code> is best-effort and does not suspend</li> </ul>"},{"location":"lifecycle-and-delivery/#shared-context-with-imprint","title":"Shared Context with <code>imprint</code>","text":"<p><code>imprint</code> adds fields to every new <code>Scroll</code> created by the same <code>Scribe</code>.</p> <pre><code>val scribe = Scribe(\n    shelf = ScrollSaver { println(it) },\n    imprint = mapOf(\n        \"app\" to JsonPrimitive(\"checkout\"),\n        \"region\" to JsonPrimitive(\"us-east-1\"),\n    )\n)\n</code></pre> <p>These values become part of the sealed scroll <code>context</code>.</p>"},{"location":"lifecycle-and-delivery/#open-and-close-hooks-with-margin","title":"Open and Close Hooks with <code>Margin</code>","text":"<p>Use <code>Margin</code> when scrolls need standard fields at creation and sealing time.</p> <pre><code>val timingMargin = object : Margin {\n    override fun header(scroll: Scroll) {\n        scroll.writeNumber(\"startedAtEpochMs\", 1000L)\n    }\n\n    override fun footer(scroll: Scroll) {\n        scroll.writeNumber(\"sealedAtEpochMs\", 2000L)\n    }\n}\n\nval scribe = Scribe(\n    shelf = ScrollSaver { println(it) },\n    margins = timingMargin,\n)\n</code></pre>"},{"location":"lifecycle-and-delivery/#graceful-shutdown","title":"Graceful Shutdown","text":"<p>There are two shutdown modes:</p> <ul> <li><code>retire()</code> closes the queue immediately</li> <li><code>planRetire()</code> closes the queue and waits for queued work to finish</li> </ul> <p>Use <code>planRetire()</code> when shutdown correctness matters more than speed.</p>"},{"location":"lifecycle-and-delivery/#uncaught-exceptions","title":"Uncaught Exceptions","text":"<p>Pass <code>onIgnition</code> to install the platform uncaught exception hook:</p> <pre><code>val scribe = Scribe(\n    shelf = EntrySaver { println(it) },\n    onIgnition = { throwable -&gt;\n        println(\"Uncaught exception: ${throwable.message}\")\n    }\n)\n</code></pre>"}]}